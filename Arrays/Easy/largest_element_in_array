class Solution {
    public int largestElement(int[] nums) {
        int largestElement = nums[0];
        for (int n = 1; n < nums.length; n++) {
            if (nums[n] > largestElement) {
                largestElement = nums[n];
            }
        }
        return largestElement;
    }
}